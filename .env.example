# ============================================================
# 🌍 Advising — Example Environment File (.env.example)
# ============================================================
# Copy this file to `.env`, fill in your values, and keep `.env`
# OUT of version control (already handled via .gitignore).
#
# Convention: boolean-like vars accept 1/0, true/false, yes/no, on/off.

# ─── Core Environment ───────────────────────────────────────
ENV=dev                        # dev / staging / prod markers for your infra
DEBUG=True                     # NEVER True in production
SECRET_KEY='replace-me'        # Use a long random string in prod
#SECRET_KEY=$(python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")

# ─── Site / Domain Settings ─────────────────────────────────
SITE_NAME=Advising             # Used in templates + email copy
SITE_DESCRIPTION="Advising Portal"  # Optional marketing blurb
SITE_ORIGIN=http://127.0.0.1:8000    # Scheme + host for absolute URLs
SITE_DOMAIN=127.0.0.1:8000     # Host:port for email links
SITE_USE_HTTPS=False           # True in production so absolute URLs use https

# ─── Host / CSRF Security ───────────────────────────────────
# REQUIRED when deploying behind Apache/Nginx.
# Comma-separated hostnames, NO scheme.
ALLOWED_HOSTS=localhost,127.0.0.1
# Full origin(s) WITH scheme. Needed so POSTs from Apache aren’t rejected.
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000
# Example production:
# ALLOWED_HOSTS=advising.example.com,www.advising.example.com
# CSRF_TRUSTED_ORIGINS=https://advising.example.com

# ─── Email Backend ──────────────────────────────────────────
# Default: store outbound email files for local dev.
EMAIL_BACKEND=django.core.mail.backends.filebased.EmailBackend
EMAIL_FILE_PATH=/absolute/path/to/tmp_emails
DEFAULT_FROM_EMAIL="Advising <no-reply@example.com>"

# Example SMTP config for SendGrid/Postmark/Mailgun:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=your-secret
# EMAIL_USE_TLS=True
# DEFAULT_FROM_EMAIL="Advising <no-reply@advising.example.com>"

# ─── Database (optional overrides) ──────────────────────────
# Defaults to SQLite (see settings.py). Uncomment/use for Postgres etc.
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=advising
# DB_USER=advising_user
# DB_PASSWORD=super-secret
# DB_HOST=127.0.0.1
# DB_PORT=5432

# ─── Redis / Cache (optional) ───────────────────────────────
# CACHE_URL=redis://127.0.0.1:6379/1

# ─── Questionnaire / Feature Flags ──────────────────────────
FORCE_QUESTIONNAIRE_COMPLETION=False    # Require questionnaire before booking

# ─── Notifications / ICS helpers ────────────────────────────
# (Emails use DEFAULT_FROM_EMAIL; ICS builder uses SITE_ORIGIN.)
# You can override these or add provider tokens as needed.

# ─── Static / Media overrides (if using S3, etc.) ───────────
# STATIC_URL=/static/
# MEDIA_URL=/media/
# AWS_STORAGE_BUCKET_NAME=your-bucket

# ─── Logging / Monitoring hooks ─────────────────────────────
# SENTRY_DSN=
# LOG_LEVEL=INFO

# ─── Misc / 3rd-party tokens ────────────────────────────────
# ANALYTICS_ID=UA-XXXXX-Y
